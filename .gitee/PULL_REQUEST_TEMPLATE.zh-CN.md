## 1. PR 目的（必填）
- 核心诉求：【例：新增图谱结构化抽取功能 / 修复非结构化文本图谱抽取异常 / 更新图谱抽取API文档】
- 关联 Issue：#xxx（必填，若无 Issue 可说明）

## 2. 实现内容（必填）
- 核心修改：
  1.  【例：新增结构化抽取接口（/kg/structured/extract），支持实体-关系自动映射
  2.  【例：修复非结构化文本分词错误，优化图谱实体边界识别逻辑】
- 设计思路（复杂功能必填）：【例：采用规则引擎+深度学习混合模型，规则引擎处理明确结构数据；抽取结果存储至Neo4j图谱数据库】

## 3. 测试情况（必填）
- 本地测试：
  1. 【例：单元测试：覆盖结构化/非结构化抽取核心逻辑，实体抽取准确率≥92%，通过率100%】
  2. 【例：功能测试：验证PDF/Word/JSON多种格式数据抽取、异常文本容错、批量数据处理场景，均符合预期】
- 测试环境：【例：关联测试图谱库验证功能正常，仅本地测试】
- 待测试点（可选）：【例：需评审者重点测试多语言文本（中英文混合）的图谱抽取效果】

## 4. 其他说明（必填，无则填“无”）
- 不兼容变更：【是/否，若是请说明：例：修改了图谱抽取接口返回格式，新增relations字段，需前端同步适配】
- 文档更新：【是/否，若是请说明：例：已更新API文档中结构化/非结构化抽取接口参数及返回示例】
- 依赖变更：【是/否，若是请说明：例：新增Neo4j驱动依赖，版本4.4.40；新增NLP分词依赖，版本3.9.3】
- 其他注意事项：【例：该功能需配置图谱数据库连接信息，已添加至配置文件模板；非结构化抽取需依赖Tika工具包】

## 5. 截图/录屏（可选）
- 【例：图谱抽取结果可视化预览截图：[图片链接]；非结构化文本抽取流程演示录屏：[视频链接]】
