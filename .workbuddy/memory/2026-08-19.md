# 2026-08-19 工作日志

## 营销稿：qKnow 开源版 v2.4.1 版本亮点
- 用户要求把三份素材（桌面 docx《非结构化抽取逻辑优化》+ 文档解析优化总结.md + 非结构化抽取营销稿.md）合并成「版本亮点 / 功能价值」结构输出。
- 实际已发布的 v2.4.1 优化主线（来自 docx）：① 非结构化抽取单段/单任务失败隔离 + Redis 并发计数回退；② 空白文档不再报错；③ 知识库文档解析单文档失败隔离 + 源头空指针防护；④ 解析按需索引（高质量索引写向量库、普通索引落库+全文检索）。
- ⚠️ 重要边界：JSON/JSONL 流式解析（Alpaca/ShareGPT/MultilingualThinking + thinking 字段）**尚未上线 v2.4.1**，不得写入本次发布亮点/对外稿。该能力代码虽已同步到 qKnow，但发布范围不含它。
